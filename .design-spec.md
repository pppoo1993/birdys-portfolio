# 项目详情 Card 规范

## 卡片容器
- 背景: `#1A1A1A`
- 边框: `1px solid #333333`
- 圆角: `4px`
- 内边距: `22px`
- 过渡: `all 0.35s cubic-bezier(0.16, 1, 0.3, 1)`
- Hover: `border-color: #3f3f46` / `translateY(-2px)` / `box-shadow: 0 20px 40px rgba(0,0,0,0.4)`

## 卡片标题
- 颜色: `#FFFFFF` / 字号: `14px` / 字重: `600`
- 下边距: `10px` / 下内边距: `10px`
- `::before`: 绿色竖条 `4px × 16px` / `#C7FF00` / `border-radius: 2px`
- `::after`: 短分割线 `80px × 1px` / `#333333` / 绝对定位底部
- 无编号 / 无 emoji / 无英文副标题

## 卡片描述
- 颜色: `#A0A0A0` / 字号: `13px` / 行高: `1.75` / 字重: `300`
- 加粗: `color: #FFFFFF` / `font-weight: 600`

## 表格
- `border-collapse: collapse`
- 单元格 padding: `24px 16px`（标签列: `24px 0`）
- 分割线: `1px solid #1f1f23`
- 文字: `13px` / `#A0A0A0` / `line-height: 1.75` / `font-weight: 300`
- 表头: `14px` / `#A0A0A0` / `font-weight: 300`

## 箭头（卡片间）
- 颜色: `#555555` / 字号: `16px` / 字重: `300`
- 间距: card `gap: 32px` / arrow `right: -24px`
